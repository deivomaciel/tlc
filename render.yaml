services:
  - type: web
    name: tlc-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: FLASK_ENV
        value: production
    plan: free  # ou outro plano de acordo com suas necessidades
